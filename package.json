{
  "name": "ips-salud-vital-2",
  "private": true,
  "version": "2.0.0",
  "description": "Sistema integral IPS Salud Vital - plataforma web y API para gestión de pacientes, citas, historias y facturación.",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix web install",
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "test": "npm --prefix server test",
    "build": "npm --prefix web run build",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
